# Gastos Compartidos

Proyecto React + Vite + Tailwind conectado a Supabase.

## Instrucciones rápidas

1. En Supabase, ejecutar `sql/supabase-init.sql` en SQL Editor.
2. Crear repo en GitHub y subir estos archivos.
3. En Netlify: conectar al repo y establecer variables de entorno:
   - VITE_SUPABASE_URL
   - VITE_SUPABASE_ANON_KEY
4. Deploy en Netlify (build command: `npm run build`, publish dir: `dist`)

